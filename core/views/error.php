<div class="container">
  <h1 class="py-2">Error 404</h1>
  <p>The requested page was not found or the URL is invalid.</p>
  Tip: <i>50.000 people used to live in this city, now it's a ghost town.</i>
  <?php
  // $pdo = new AnyModel();
  // $query = "SELECT datname FROM pg_database;";
  // $result = $pdo->custom_query($query);

  // print_r($result->query_result);
  ?>
</div>